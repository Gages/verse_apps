dist: trusty
sudo: false
language: c

addons:
  apt:
    packages:
      - libfuse-dev
      - pkg-config
      - fuse
  
before_script:
  - git clone git://github.com/gittup/tup.git
  - cd tup
  - ./bootstrap.sh
script:
  - tup variant tupconf/default.config
  - tup

