.gitignore
ROOT=$(TUP_CWD)/source
UP_CFLAGS=-Wno-deprecated-declarations
#UP_CFLAGS+=-Werror -Wall

ifndef CC
    error "CC must be set in the config file"
endif

ifndef CC_NATIVE
    error "CC_NATIVE must be set in the config file"
endif
